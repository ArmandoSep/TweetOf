# TweetOf


## Instalation

Download the reopo and navigate there from the command line:

```sh
```

## Setup

```sh
pipenvshell install
pipenv shell
```

## Usage

Run the web app:

```sh
FLASK_APP=web_app flask run
```